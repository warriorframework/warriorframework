<?xml version="1.0"?>

<!-- To read full documentation for this file, please refer to user_generated/config_sample.xml -->

<data>
    <warhorn name="Warhorn">
        <dependency name="jira" install="no"/>
        <dependency name="lxml" install="yes"/>
        <dependency name="ncclient" install="no"/>
        <dependency name="paramiko" install="yes"/>
        <dependency name="pexpect" install="yes"/>
        <dependency name="pysnmp" install="yes"/>
        <dependency name="requests" install="yes"/>
        <dependency name="selenium" install="yes"/>
        <dependency name="xlrd" install="no"/>
        <dependency name="cloudshell-automation-api" install="yes"/>
    </warhorn>

    <!-- Fill this out with the details of the virtual environment 
         to be used. Please note that if this is not used, then the warhorn
         expects the dependency packages to be already installed or
         admin privileges available for installing them. location is the 
         place where virtualenv binary can be found. The virtual env
         would be created at the warrior main destination path.
         If install is set to yes, then warhorn expects atleast the
         admin privileges to install virtual env.
         -->

    <virtualenv name="warrior_virtualenv" activate="yes"
        location="~/.local/bin/virtualenv" install="no" />

    <warrior url="ssh://git@rtx-swtl-git.fnc.net.local:7999/war/warriorframework.git"
        destination="" label="" clean_install="" clone="yes">
    </warrior>

    <drivers>

        <!--

        Fill this out with the details of the repository that you want to clone

        <repository url="http://repository/one/url.git" clone="yes" label="" all_drivers="no">
            <driver name="driver_one_name" clone="yes"/>
			<driver name="driver_two_name" clone=""/>
			<driver name="driver_three_name" clone="no"/>
			<driver name="driver_four_name"/>
        </repository>

        -->

    </drivers>

    <warriorspace>

        <!--

        Fill this out with the details of the repository that you want to clone

        <repository url="http://warriorspace/repository/url.git"
        label="f455scd00i" clone="">
        </repository>

        -->

    </warriorspace>
</data>
